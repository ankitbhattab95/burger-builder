{
  "name": "burger-full",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tart:server": "npx nodemon server.js",
    "tart:client": "cd burger-builder && npm run start",
    "tart:dev": "concurrently \"npm run start:server \" \"npm run start:client\"",
    "installPackage": "npm i && npm i --prefix ./burger-builder",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix burger-builder && npm run build --prefix burger-builder"
  },
  "proxy": "http://localhost:4000",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.2.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.4"
  }
}